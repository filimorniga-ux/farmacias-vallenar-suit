import { pool } from './src/lib/db'; async function main() { const res = await pool.query('SELECT * FROM product_suppliers LIMIT 1'); console.log(Object.keys(res.rows[0] || {})); process.exit(0); } main();
